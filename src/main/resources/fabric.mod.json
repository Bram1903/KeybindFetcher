{
  "schemaVersion": 1,
  "id": "translatable-fetcher",
  "version": "${version}",
  "name": "Translatable Fetcher",
  "description": "Fetches all translatable keys in the game.",
  "author": "Bram",
  "environment": "client",
  "entrypoints": {
    "client": [
      "com.deathmotion.translatablefetcher.TranslatableFetcher"
    ]
  },
  "mixins": [
    "TranslatableFetcher.mixins.json"
  ],
  "depends": {
    "fabricloader": ">=${loader_version}",
    "minecraft": ">=1.21.11",
    "fabric-api": "*"
  }
}
